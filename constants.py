"""Compatibility exports; implementation lives in gpt_rag_ui.util.constants."""

from gpt_rag_ui.util.constants import (
    APPLICATION_INSIGHTS_CONNECTION_STRING as APPLICATION_INSIGHTS_CONNECTION_STRING,
    APP_NAME as APP_NAME,
    REFERENCE_REGEX as REFERENCE_REGEX,
    SUPPORTED_EXTENSIONS as SUPPORTED_EXTENSIONS,
    TERMINATE_TOKEN as TERMINATE_TOKEN,
    UUID_REGEX as UUID_REGEX,
)
